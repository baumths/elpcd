name: storage_service
description: The storage interface for the ElPCD app.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.1.3

dependencies:
  elpcd_utils:
    path: ../elpcd_utils

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
