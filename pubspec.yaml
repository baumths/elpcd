name: elpcd
description: Ferramenta para criação de PCDs e TTDs.

version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_svg: 0.22.0

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/images/gedalogo_480x320.png
  uses-material-design: true
